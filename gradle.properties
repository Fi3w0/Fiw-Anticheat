# Shared properties for all modules.
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.daemon=true

# Mod identity (shared)
maven_group=dev.fiw
mod_version=2.0.2
archives_base_name=fiw-anticheat
